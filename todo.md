1. Finalize Draft, when the last pick is made, the draft should be marked as complete, and then it is removed from the home index. (should probably go right into the "start season" in which the schedule + matchups would be generated.)

2. There is a bug with some of the draft picks, it looks like a few of the later picks are not marking as rostered for some reason. It is correct in "available players" in the draft page, but not in the free agents. Seems like this is just an issue with not grabbing the information quickly enough. When the app is reloaded this issue goes away. Should "available players" and "free agent list" be more intertwined?

3. Game data: date and opponent.

4. Player data: pictures

5. Tooltips for fields in league creation/ or a key to explain things.

6. swipe to the side on the different Teams/Leagues in the popdown switcher, to be able to favorite one and make it the default league when you open the app. (give it a visual indicator that it has been chosen as the favorite)

7. Add IR slots to team.

8. scrollwheel should not appear in the swap roster modal popup.

9. leagues that are full shouldnt be options to add in the "join a league"

- Turn Add/Drop into an edge function, needs to check against roster size, roster constraints, so decent sized compuations that should be done not on client i expect.
